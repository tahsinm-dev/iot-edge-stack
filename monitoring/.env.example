# Copy to .env and adjust. Never commit the real .env (it is git-ignored).
MQTT_USER=iot
MQTT_PASSWORD=changeme
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin
# Host port for Grafana (change if 3000 is already in use)
GRAFANA_PORT=3000
