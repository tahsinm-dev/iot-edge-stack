# Mosquitto broker - authenticated.
# Deploy to: /etc/mosquitto/conf.d/iot.conf
# Create the password file once:  sudo mosquitto_passwd -c /etc/mosquitto/passwd iot
listener 1883
persistence true
persistence_location /var/lib/mosquitto/
log_dest syslog

allow_anonymous false
password_file /etc/mosquitto/passwd
