# -> /etc/wpa_supplicant/wpa_supplicant.conf   (EDGE wlan0 = Client zum SERVER)
country=AT
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
network={
    ssid="SERVER-IOT-XX"
    psk="__WPA_SERVER__"
}
