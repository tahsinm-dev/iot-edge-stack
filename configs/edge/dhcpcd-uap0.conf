# An /etc/dhcpcd.conf ANHAENGEN (EDGE) — statische IP fuer den virtuellen AP uap0.
# WICHTIG: fuer wlan0 KEINEN statischen Block setzen (wlan0 bleibt DHCP-Client des SERVERs).
interface uap0
static ip_address=192.168.166.1/24
nohook wpa_supplicant
