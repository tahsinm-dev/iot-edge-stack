---
# Copy to secrets.yml, fill in real values and encrypt it:
#   ansible-vault encrypt secrets.yml
# secrets.yml is git-ignored.
edge_wifi_password: "change-me"
server_wifi_password: "change-me"
mqtt_password: "change-me"
